name: Publish

# Runs on every v* tag: checks the tag against the package version, builds
# the gem, attaches it to a GitHub Release for the tag, and pushes it to
# RubyGems.org only when the RUBYGEMS_API_KEY repository secret exists.
# Without the secret the push is skipped with a notice and the gem is pushed
# by hand (GEM_HOST_API_KEY=… gem push skaidb-<version>.gem); the release
# asset mirrors what is on https://rubygems.org/gems/skaidb.

on:
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
      - name: Tests
        run: ruby -w -Ilib -Itest -e 'Dir["test/*_test.rb"].sort.each { |f| require File.expand_path(f) }'
      - name: Tag must equal the package version
        run: |
          tag="${GITHUB_REF_NAME#v}"
          ver="$(ruby -Ilib -e 'require "skaidb"; print Skaidb::VERSION')"
          if [ "$tag" != "$ver" ]; then
            echo "::error::tag v$tag does not match Skaidb::VERSION $ver"
            exit 1
          fi
          echo "VERSION=$ver" >> "$GITHUB_ENV"
      - name: Build the gem
        run: |
          gem build skaidb.gemspec
          ls -l "skaidb-$VERSION.gem"
          gem specification "skaidb-$VERSION.gem" version | grep -q "$VERSION"
      - name: Release notes from CHANGELOG.md
        run: |
          awk -v v="$VERSION" '
            $0 ~ "^## \\[" v "\\]" { on = 1; next }
            on && /^## \[/ { exit }
            on { print }
          ' CHANGELOG.md > release-notes.md
          if [ ! -s release-notes.md ]; then echo "Release $VERSION" > release-notes.md; fi
          printf '\n---\nInstall: `gem install skaidb` (https://rubygems.org/gems/skaidb), or `gem "skaidb", "~> 1.0"` in a Gemfile. The built `skaidb-%s.gem` is attached below for installs without registry access (`gem install ./skaidb-%s.gem`).\n' "$VERSION" "$VERSION" >> release-notes.md
      - name: GitHub Release with the gem attached
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ env.VERSION }}
          body_path: release-notes.md
          files: skaidb-${{ env.VERSION }}.gem
          fail_on_unmatched_files: true
      - name: Push to RubyGems.org (only when RUBYGEMS_API_KEY is set)
        env:
          GEM_HOST_API_KEY: ${{ secrets.RUBYGEMS_API_KEY }}
        run: |
          if [ -z "$GEM_HOST_API_KEY" ]; then
            echo "::notice title=RubyGems push skipped::RUBYGEMS_API_KEY secret is not set; nothing was pushed. Push by hand: GEM_HOST_API_KEY=… gem push skaidb-$VERSION.gem (https://rubygems.org/gems/skaidb)"
            exit 0
          fi
          gem push "skaidb-$VERSION.gem"
